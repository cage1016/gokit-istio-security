FROM gcr.io/distroless/base
ARG BINARY
ARG COMMIT_HASH
ARG BUILD_TIMESTAMP
ARG VERSION

LABEL CommitHash=${COMMIT_HASH} BuildTimestamp=${BUILD_TIMESTAMP} VERSION=${VERSION}

COPY $BINARY /exe
ENTRYPOINT ["/exe"]
